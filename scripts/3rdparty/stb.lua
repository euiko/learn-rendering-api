-- stb = dependency("3rdparty", "stb", true)
--     kind    "StaticLib"

    STB_DIR = path.join(LRA_3RDPARTY, "stb")
    STB_INC_DIR = STB_DIR

    -- files {
    --     -- path.join(STB_DIR, "*.h")
    -- }

    -- includedirs {
    --     STB_INC_DIR
    -- }